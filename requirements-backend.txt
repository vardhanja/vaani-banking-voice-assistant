# Backend API Dependencies Only
# Minimal requirements for backend deployment on Vercel
# Optimized to reduce memory usage during build

# Core FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
starlette>=0.27.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Data validation and serialization
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Authentication and security
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0

# HTTP and form handling
python-multipart>=0.0.12
httpx==0.27.2

# Packaging helpers
setuptools>=68.0.0

# Utilities
python-dotenv==1.0.1
tenacity==8.5.0

# Logging
structlog==24.4.0

# Note: Removed test dependencies (pytest, etc.) to reduce build memory
# Note: Removed faker (only needed for seeding, not production)

