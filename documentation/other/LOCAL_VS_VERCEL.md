# Vercel references removed

This document previously compared local development vs Vercel deployment. Vercel-specific instructions and references have been removed during repository cleanup.

If you need a platform-specific comparison (e.g., Docker, Dokku, Render, Netlify), please add a new document under `documentation/` and specify the target platform.
## Local Development

