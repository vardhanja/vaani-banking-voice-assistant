# Minimal requirements for Vercel deployment (OpenAI-only)
# Excludes heavy ML dependencies that aren't needed when using OpenAI

# Core AI Framework (minimal - only what's needed for OpenAI)
langchain>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0
langsmith>=0.1.0

# OpenAI (via httpx - no need for openai package if using langchain)
# Note: OpenAI is accessed via httpx in openai_service.py

# API & Web
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.12
python-jose[cryptography]>=3.3.0

# HTTP Client (for OpenAI API)
httpx==0.27.2

# Utilities
python-dotenv==1.0.1
tenacity==8.5.0

# Logging & Monitoring
structlog==24.4.0
sentry-sdk==2.15.0

# Database & ORM (for backend integration)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Vector Database (library only - database files excluded via .vercelignore)
# Note: ChromaDB library is needed for imports, but actual DB files are built on first use
chromadb>=0.4.22
langchain-chroma>=0.1.2

# Document Processing (needed for RAG - PDF loading)
pypdf>=3.17.0

# Note: The following are EXCLUDED to reduce size:
# - ollama (not needed for OpenAI)
# - sentence-transformers (huge ML models - RAG uses OpenAI embeddings instead)
# - langchain-ollama (not needed for OpenAI)
# - redis (optional, not critical)
# - azure-cognitiveservices-speech (optional TTS)
# - ChromaDB database files (*.bin, *.sqlite3) - built on first use from PDFs

