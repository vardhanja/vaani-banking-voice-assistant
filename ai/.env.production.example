# AI service production environment example
# Copy to ai/.env.production (not committed) and fill real values.

# LLM configuration (OpenAI in serverless deployment)
LLM_PROVIDER=openai
OPENAI_API_KEY=<openai-key>
OPENAI_MODEL=gpt-4o-mini

# Guardrails
ENABLE_INPUT_GUARDRAILS=true
ENABLE_OUTPUT_GUARDRAILS=true

# API server
API_HOST=0.0.0.0
API_PORT=8001
API_RELOAD=false

# CORS overrides
CORS_ALLOWED_ORIGINS=https://sunnationalbank.online,https://www.sunnationalbank.online

# Logging
LOG_LEVEL=INFO
